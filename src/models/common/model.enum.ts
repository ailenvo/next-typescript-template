export enum Roles {
  SuperAdmin = "SuperAdmin",
  Admin = "Admin",
  Customer = "Customer",
}

export enum ObjectStatus {
  Active = "active",
  DeActive = "deactive",
}
export enum OrderDirection {
  DESC = "DESC",
  ASC = "ASC",
}
export enum EImageType {
  png = ".png",
  jpeg = ".jpeg",
  jpg = ".jpg",
}

export enum ECurrencyCode {
  VND = "VND",
  USD = "USD",
  EUR = "EUR",
}

export enum ELanguage {
  Vie = "vi-VN",
  Eng = "en-US",
}

export enum EModeModal {
  CREATE = "CREATE",
  UPDATE = "UPDATE",
  UPDATE_STATUS = "UPDATE_STATUS",
  DELETE = "DELETE",
  NOTHING = "NOTHING",
}

export enum ELocalStorageName {
  UserToken = "token",
  User = "user",
}
